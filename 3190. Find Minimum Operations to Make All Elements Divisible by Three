class Solution {
public:
    int minimumOperations(vector<int>& nums) {
        int count=0;
        for(int i=0;i<nums.size();i++){
            int t=nums[i]%3;
            int y=3-t;
            count+=min(y,t);
        }
        return count;
    }
};
