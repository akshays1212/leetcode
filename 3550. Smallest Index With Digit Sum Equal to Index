class Solution(object):
    def smallestIndex(self, nums):
        for i in range(len(nums)):
            r = 0
            y = nums[i]
            
            while y > 0:
                r += y % 10
                y = y // 10
                
            if i == r:
                return i
                
        return -1
